#pragma once

// For basic cin, cout etc
#include <iostream>

// For string data type and operations
#include <string>

// For doing math operations like sqrt
#include <math.h>

// For dealing with files used for stl reading and parameter reading
#include <fstream>

// For Vector data type
#include <vector>

// Used to convert string to stringstream do split strings
#include <sstream>

// For map data type used in extracting paramters from file
#include <map>

// For Bitset data type in byte convertors
#include <bitset>

using namespace std;